name: no_new_priv
docker_args:
  image: "ubuntu:latest"
  command: "sleep infinity"
  detach: True
  tty: True 
  security_opt:
    - "apparmor=docker-default"
command_to_execute:
  command: ["bash", "-c", "mknod /dev/fake c 1 3"]