#!/bin/bash

TARGET_DIR=$1  # Buildroot passes the target root filesystem directory as the first argument
echo $TARGET_DIR
# cp ../server.c "${TARGET_DIR}/etc/server.c"
# cp ../server.h "${TARGET_DIR}/etc/server.h"