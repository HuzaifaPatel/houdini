How to make root filesystem

dd if=/dev/zero of=rootfs bs=1G count=10

sudo mkfs.ext4 rootfs

mkdir rootfs

cd rootfs

sudo debootstrap --variant=minbase focal . http://archive.ubuntu.com/ubuntu/


```
set default="0"
set timeout="5"

menuentry "Buildroot" {
	linux /boot/bzImage root=/dev/sda rootwait console=tty1
}

```

sudo qemu-system-x86_64 -kernel /YOURPATH/bzImage -hda /YOURPATH/rootfs -append "root=/dev/sda console=ttyS0" -nographic -m 10000 -enable-kvm


sudo qemu-system-x86_64 -kernel cache/kernels/6.2/linux-6.2/arch/x86/boot/bzImage -drive file=image/ubuntu.img,format=raw,index=0,media=disk -append "root=/dev/sda console=ttyS0" -nographic -m 8000 -enable-kvm -net nic -net user -smp cores=4,maxcpus=4


https://sources.buildroot.net/